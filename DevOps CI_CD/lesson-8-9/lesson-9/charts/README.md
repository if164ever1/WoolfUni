# Submission copy of the GitOps chart

The authoritative runtime copy belongs in the separate `lesson-8-9-gitops` repository. This duplicate is included in the primary assignment repository because the requested project structure explicitly contains `charts/django-app/`.

Jenkins updates the **separate GitOps repository**, not this duplicate.
